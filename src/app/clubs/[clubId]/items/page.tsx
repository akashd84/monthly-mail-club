import { AppShell } from "@/components/layout/app-shell";import { getCurrentArtist } from "@/lib/auth/session";import { prisma } from "@/lib/db/prisma";import { archiveItem, createItem } from "@/server/actions/plans-items";
export default async function Items({params}:{params:Promise<{clubId:string}>}){const {clubId}=await params; const artist=await getCurrentArtist(); await prisma.club.findFirstOrThrow({where:{id:clubId,artistAccountId:artist.id}}); const items=await prisma.clubItem.findMany({where:{clubId},orderBy:{createdAt:"desc"}}); return <AppShell><h1 className="text-3xl font-bold">Included Items</h1><form action={createItem.bind(null,clubId)} className="card mt-6 grid gap-3 md:grid-cols-3"><input className="input" name="name" placeholder="Sticker, ACEO, postcard..."/><select className="input" name="itemType"><option>STICKER</option><option>ACEO</option><option>PRINT</option><option>POSTCARD</option><option>BOOKMARK</option><option>OTHER</option></select><button className="btn">Add item</button></form><table className="table mt-6"><tbody>{items.map(i=><tr key={i.id}><td>{i.name}</td><td>{i.itemType}</td><td>{i.isActive?"Active":"Archived"}</td><td><form action={archiveItem.bind(null,clubId,i.id)}><button className="btn-secondary">Archive</button></form></td></tr>)}</tbody></table></AppShell>}
