import { AppShell } from "@/components/layout/app-shell";import { getCurrentArtist } from "@/lib/auth/session";export default async function Account(){const artist=await getCurrentArtist(); return <AppShell><h1 className="text-3xl font-bold">Account</h1><div className="card mt-6"><p><b>Display name:</b> {artist.displayName}</p><p><b>Public slug:</b> {artist.publicSlug}</p><p className="mt-3 text-sm text-slate-600">Auth is ready for Better Auth integration; this MVP uses the development session scaffold.</p></div></AppShell>}
