import Link from "next/link";import { AppShell } from "@/components/layout/app-shell";import { getCurrentArtist } from "@/lib/auth/session";import { prisma } from "@/lib/db/prisma";
export default async function Dashboard(){const artist=await getCurrentArtist(); const [clubs,subs,runs]=await Promise.all([prisma.club.count({where:{artistAccountId:artist.id,status:"ACTIVE"}}),prisma.subscriber.count({where:{club:{artistAccountId:artist.id},status:"ACTIVE"}}),prisma.mailRun.count({where:{club:{artistAccountId:artist.id}}})]); return <AppShell><h1 className="text-3xl font-bold">Dashboard</h1><div className="mt-6 grid gap-4 md:grid-cols-3">{[["Active clubs",clubs],["Active subscribers",subs],["Mail runs",runs]].map(([l,v])=><div className="card" key={l}><p className="text-sm text-slate-600">{l}</p><p className="mt-2 text-3xl font-bold">{v}</p></div>)}</div><Link className="btn mt-6" href="/clubs/new">Create a club</Link></AppShell>}
